{
  "name": "zkone",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio --version-label v14 zkone",
    "create-local": "graph create --node http://localhost:8020/ zkone",
    "remove-local": "graph remove --node http://localhost:8020/ zkone",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 zkone",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.45.0",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
